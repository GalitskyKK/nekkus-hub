# Windows runner = amd64. CGO для webview/systray.
builds:
  - id: nekkus-hub
    main: ./cmd
    binary: nekkus-hub
    goos:
      - windows
    goarch:
      - amd64
    env:
      - CGO_ENABLED=1
